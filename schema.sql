CREATE DATABASE student_demo_registration_db;
USE student_demo_registration_db;

CREATE TABLE first_timeslot (
    ID int,
    fname varchar(255),
    lname varchar(255),
    project_name varchar(255),
    email_address varchar(255),
    phone_number varchar(255),
    time_slot varchar(255)
);